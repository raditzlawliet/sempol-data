{
  "name": "sempol-data",
  "scripts": {
    "deploy-cf": "mkdir builds && cp data/* builds",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^0.34.5",
    "luxon": "^3.4.3",
    "mathjs": "^11.11.1",
    "vitest": "^0.34.4"
  }
}
